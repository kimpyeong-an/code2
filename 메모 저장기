filename = input("파일 이름을 입력하세요 (예: memo.txt): ")
print("메모 내용을 입력하세요. 종료하려면 빈 줄에서 Enter.")

lines = []
while True:
    line = input()
    if line == "":
        break
    lines.append(line)

with open(filename, "w", encoding="utf-8") as f:
    f.write("\n".join(lines))

print(f"{filename}에 저장되었습니다.")
